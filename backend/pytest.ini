# pytest 配置文件

[pytest]
# 测试文件搜索模式
testpaths = tests

# 异步测试模式
asyncio_mode = auto

# 输出格式：-v 显示每个测试名，-s 允许 print 输出
addopts = -v --tb=short

# 过滤警告
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
